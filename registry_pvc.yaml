apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crc-image-registry-storage
  namespace: openshift-image-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
